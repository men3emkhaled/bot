[project]
name = "albalashon-telegram-bot"
version = "0.1.0"
requires-python = "~=3.12.0"
dependencies = [
    "APScheduler==3.10.4",
    "Flask==3.0.3",
    "psycopg2-binary==2.9.9",
    "python-dotenv==1.0.1",
    "python-telegram-bot==20.8",
]

[tool.vercel]
entrypoint = "api.telegram:app"
